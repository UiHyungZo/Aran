{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "mkdir -p .claude/logs && (git diff --name-only | grep -E '\\.swift$' | grep -q . && nohup bash -lc 'swiftformat . && swiftlint' > .claude/logs/post-tool-use.log 2>&1 &)"
          }
        ]
      }
    ],
    "Notification": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "osascript -e 'display notification \"작업 확인 필요\" with title \"Claude Code\"'"
          }
        ]
      }
    ]
  }
}
